import { NextResponse } from "next/server";import { PERMISSIONS,requirePermission } from "@/lib/auth/permissions";import { CollaboratorValidationError,mergeCollaborators } from "@/modules/collaborators/server";
export async function POST(request:Request){const{response}=await requirePermission(PERMISSIONS.MASTER_DATA_MANAGE);if(response)return response;try{const{primaryId,secondaryId}=await request.json();return NextResponse.json(await mergeCollaborators(primaryId,secondaryId));}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Falha na mesclagem."},{status:error instanceof CollaboratorValidationError?400:500});}}
