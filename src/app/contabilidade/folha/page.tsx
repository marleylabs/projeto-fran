import { PayrollWorkspace } from "@/modules/accounting/payroll";

/** Rota canônica do módulo atual durante a transição para o Financeiro Global. */
export default function AccountingPayrollPage() {
  return <PayrollWorkspace />;
}
